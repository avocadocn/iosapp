ion-view
  ion-header-bar
    a.button.icon-left.ion-chevron-left(ng-click="goBack()") &nbsp;返回
    h1.title {{photoAlbum.name}}
    .button.icon.ion-plus(ng-click="showUploadActionSheet()")

  ion-content.photo_album_detail_page
    .dl_photo_album_card(ng-repeat="group in photoGroups" on-last-repeat)
      .card_header.clearfix
        p.header_title {{group.date | date:'yyyy年MM月dd日'}}
      .dl_gallery.clearfix
        img.photo(ng-repeat="photo in group.photos" ng-src="{{STATIC_URL + photo.uri}}/65/65" photo="photo" pswp-id="pswpId" photos="photos" open-photo)
    .list(ng-if="photoGroups.length === 0")
      .item 该相册还没有照片，快来上传吧。

  script(id="confirm-upload-modal.html" type="text/ng-template")
    ion-modal-view
      ion-header-bar
        button.button(ng-click="cancelUpload()") 取消
        h1.title 预览
        button.button(ng-click="confirmUpload()") 确定
      ion-content
        .card
          .item.item-image
            img(ng-src="{{previewImg}}")

 include ./photo-swipe
