ion-view
  ion-header-bar
    a.button.icon-left.icon.ion-chevron-left(ng-href="{{backUrl}}") &nbsp;返回
    h1.title 用户信息
  ion-content
    include ./timeline-card
    ion-infinite-scroll(ng-if="!loading && !loadFinished" on-infinite="loadMore()" distance="5%")
    .nomore_text(ng-if="loadFinished") 没有更多的足迹
